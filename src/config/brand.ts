// ───────────────────────────────────────────────────────────
// Brand config — single source of truth for product naming.
// Swap BRAND.name here to rename the whole product.
// ───────────────────────────────────────────────────────────

export const BRAND = {
  name: "NullFi",
  tagline: "Trust-Minimized Finance",
  chain: "Sui",
  /** stablecoin used for display */
  defaultToken: "USDC",
} as const;
